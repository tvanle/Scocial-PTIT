server:
  port: 8083
spring:
  application:
    name: comment-service
  datasource:
    url: jdbc:postgresql://postgres:5432/ptit_social_comment
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka/
